// En funktion som tar emot två värden och returnerar en triangels hypotenusa genom pythagoras-sats  

function triangel(a, b) {
    return Math.pow(a, 2) + Math.pow(b, 2);
} 
console.log(triangel(2,5));